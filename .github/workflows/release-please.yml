on:
  push:
    branches:
      - prod

name: release-please

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.MY_RELEASE_PLEASE_TOKEN }}
          release-type: simple
          target-branch: prod

  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - run: mvn -B clean package -DskipTests
        working-directory: App

      - run: mkdir -p deploy/target && cp App/Dockerfile deploy/ && cp App/docker-compose.yml deploy/ && cp App/target/*.jar deploy/target/

      - uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          source: deploy/
          target: /opt/emovieshop/

      - uses: appleboy/ssh-action@v1
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          AUTH0_ISSUER_URI: "https://${{ secrets.AUTH0_DOMAIN }}/"
          AUTH0_AUDIENCE: "https://emovieshop-api"
          AUTH0_MGMT_DOMAIN: ${{ secrets.AUTH0_DOMAIN }}
          AUTH0_MGMT_AUDIENCE: "https://${{ secrets.AUTH0_DOMAIN }}/api/v2/"
          AUTH0_MGMT_CLIENT_ID: ${{ secrets.AUTH0_CLIENT_ID }}
          AUTH0_MGMT_CLIENT_SECRET: ${{ secrets.AUTH0_CLIENT_SECRET }}
          AUTH0_ROLE_ID_ADMIN: ${{ secrets.AUTH0_ROLE_ID_ADMIN }}
          AUTH0_ROLE_ID_SUPPORT: ${{ secrets.AUTH0_ROLE_ID_SUPPORT }}
          AUTH0_ROLE_ID_CUSTOMER: ${{ secrets.AUTH0_ROLE_ID_CUSTOMER }}
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          envs: DB_HOST,DB_PORT,DB_NAME,DB_USERNAME,DB_PASSWORD,AUTH0_ISSUER_URI,AUTH0_AUDIENCE,AUTH0_MGMT_DOMAIN,AUTH0_MGMT_AUDIENCE,AUTH0_MGMT_CLIENT_ID,AUTH0_MGMT_CLIENT_SECRET,AUTH0_ROLE_ID_ADMIN,AUTH0_ROLE_ID_SUPPORT,AUTH0_ROLE_ID_CUSTOMER
          script: |
            mkdir -p /opt/emovieshop/receipts
            docker build -t emovieshop:latest /opt/emovieshop/deploy
            cd /opt/emovieshop/deploy
            docker compose up -d
            docker image prune -f
